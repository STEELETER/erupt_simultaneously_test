import os
from winxpgui import IsWindow, IsWindowEnabled, GetWindowText, IsWindowVisible, EnumWindows

# title_set = set()
# def foo(hwnd,mouse):
#
#     if IsWindow(hwnd) and IsWindowEnabled(hwnd) and IsWindowVisible(hwnd):
#         title_set.add(GetWindowText(hwnd))
#
# EnumWindows(foo, 0)
# lt = [t for t in title_set if t]
# for t in lt:
#     if 'fliename_ls' == t:
#         path = os.path.abspath(t)
#         print(path)

    # 监听 标记文件 判断如果路径存在 存在不下载 路径不存在 开始下载
    # 重新获取 标记文件的路径 该路径就为 我们想要的路径 将路径返回

    # sufix = os.path.splitext(t)[1][1:]
    # if sufix == 'fliename_ls':
    #     flie_path = os.path.abspath(t)
    #     print(flie_path)
    #     print(t)
    #     print('开始下载.....')







